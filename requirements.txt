# GLo-MAPPO — Python requirements
# Tested with Python 3.11 (the exact versions used to produce the paper's results).
# Install into a fresh virtual environment (see README.md, "Setup").

# --- Core: deep RL + numerics ---
torch==2.5.1
torchvision==0.20.1
numpy==1.24.4
scipy==1.15.1

# --- MARL environment + experiment framework ---
gym==0.22.0            # env API -- use THIS version (0.26 changes the reset/step signature)
sacred==0.8.7          # experiment runner (src/main.py)
PyYAML==6.0.1          # YAML configs

# --- Data handling + plotting (trajectory / association CSVs, figures) ---
pandas==2.0.3
matplotlib==3.10.0
Pillow==11.1.0

# --- Logging / utilities ---
tensorboard-logger==0.1.0
click==8.1.8
protobuf==3.20.3
six==1.17.0
python-dateutil==2.9.0.post0

# --- Optional (only for certain baselines/modules; NOT required for GLo-MAPPO) ---
# einops               # some critic/agent variants
# torch-scatter        # QTRAN / DCG critics
# imageio==2.37.0      # gif/video helpers
