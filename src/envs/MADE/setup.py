from setuptools import setup

setup(name= 'gym_mdde1', # name of the package
      version='0.0.2',  # version of this release
      install_requires=['gym>=0.2.0'] # specifies the minimal list of libraries required to run the package correctly
      )